// code your solution here
function superbowlWin(record) {
    record.find(result)
        if (result = "W") {
            return year
        }
        else {
            return 'Undefined'
        }
}
superbowlWin();